//! arc-filesystem — workspace indexing, file watch, semantic search (stub).
//!
//! Phase 2: notify-based watcher + tantivy index. For now this crate is a
//! placeholder so the workspace topology is fixed.

pub fn placeholder() {}
