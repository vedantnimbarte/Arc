[package]
name = "arc-dap"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Debug Adapter Protocol client for ARC — spawns and drives debug adapters over stdio or TCP."

[dependencies]
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
dashmap = { workspace = true }
# Only for `assign_to_job` — the Windows kill-on-close job that reaps
# adapters if arc.exe dies without running `stop_all`.
arc-pty = { workspace = true }
