// Terminal package — extracted xterm.js wiring + command-block parsing.
//
// Phase 1: the Terminal component still lives in apps/frontend. When we have
// a second consumer (e.g. a popout/standalone window) we'll move it here.

export const TERMINAL_PACKAGE_PLACEHOLDER = true;
